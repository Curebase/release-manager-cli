// Merge the release branch into master and develop
// await git.checkout('master');
// await git.mergeFromTo(releaseBranch, 'master', ['--no-ff']);
// await git.checkout('develop');
// await git.mergeFromTo(releaseBranch, 'develop', ['--no-ff']);
